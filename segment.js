module.exports = function() {
    this.fields = [];

    this.update = function(location, field) {
        if (location == 0 && data.length != 3)
            return new Error('First component must be the segment header');
            
        // TODO
    };
};
