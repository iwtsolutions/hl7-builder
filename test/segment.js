var should = require('should');
var Segment = require('../segment');
var Field = require('../field');

describe('Segment', function() {
    describe('update(location, field)', function() {
        it('should add a field at location 6 and add other fields before it');

        it('should error if the length of data is not equal to 3 at location 0');

        it('should add a field at location 0 with al ength of 3 and capitalize it');
    });
});
